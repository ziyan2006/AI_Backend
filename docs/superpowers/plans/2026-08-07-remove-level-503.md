# 移除进位汇聚关卡实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 同步删除固件与后端的旧 503，并将原 504 全加器迁移为 503「全加主控」。

**架构：** 后端规则文件是固件生成规则的事实源；先用测试锁定新编号和名称，再修改规则、评测和提示词，随后同步固件关卡树、教程和自检。存档继续按关卡 ID 工作，不把旧 503 完成状态迁移给新的全加器。

**技术栈：** Python、Pytest、Pydantic、ESP-IDF、C、Ninja。

---

### 任务 1：锁定后端关卡集合

**文件：**
- 修改：`tests/test_level_logic.py`
- 修改：`tests/test_evaluation.py`

- [ ] 增加断言：503 的输入/输出数量为 3/2，目标是真正的全加器真值表。
- [ ] 增加断言：`get_level_logic_spec(504, 1)` 抛出 `KeyError`。
- [ ] 更新评测关卡 ID 列表，使 501、502 后直接出现 503。
- [ ] 运行相关测试并确认旧实现失败。

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_level_logic.py tests\test_evaluation.py -q
```

### 任务 2：迁移后端规则与引导

**文件：**
- 修改：`src/tuco_ai_backend/data/level_logic_specs.json`
- 修改：`src/tuco_ai_backend/evaluation.py`
- 修改：`src/tuco_ai_backend/providers/openai_compatible.py`
- 修改：`src/tuco_ai_backend/evaluation_cli.py`
- 修改：`src/tuco_ai_backend/evaluation_presets.py`
- 修改：`src/tuco_ai_backend/frontend/index.html`
- 修改：`src/tuco_ai_backend/frontend/app.js`
- 修改：`tests/test_circuit_coach_v2.py`
- 修改：`tests/test_circuit_planner.py`
- 修改：`tests/test_evaluation_scenarios.py`
- 修改：`tests/test_llm_service.py`

- [ ] 删除旧 503 三输入或门规则。
- [ ] 将原 504 全加器规则、学习活动、提示词和测评预设改为 503。
- [ ] 将名称改为 501「个位引擎」、502「进位引擎」、503「全加主控」。
- [ ] 对仅用于三输入或门算法测试的场景改用测试内自定义规则，不保留生产 503 依赖。
- [ ] 运行后端针对性测试，确认迁移通过。

### 任务 3：迁移固件关卡树与规则

**文件：**
- 修改：`E:\emb_agent_new\main\campaign_content.c`
- 修改：`E:\emb_agent_new\main\level_rules.generated.inc`
- 修改：`E:\emb_agent_new\main\level_intro.c`
- 修改：`E:\emb_agent_new\main\game_logic_self_test.c`
- 修改：`E:\emb_agent_new\main\app_ui.c`

- [ ] 删除旧 503 节点，将原 504 节点改为 503、前置改为 502，并移动到旧 503 的地图位置。
- [ ] 将奖励节点 550 的前置从 504 改为 503。
- [ ] 将原 504 教程入口和学习小游戏映射迁移到 503。
- [ ] 将固件生成规则改为新的 503 全加器，并更新自检。
- [ ] 保留所有助教诊断相关未提交改动。

### 任务 4：同步文档并验证

**文件：**
- 修改：`docs/implementation.md`
- 修改：`PROJECT_HANDOFF.md`
- 修改：`E:\emb_agent_new\开发日志.md`

- [ ] 更新当前关卡顺序、名称和学习活动支持列表。
- [ ] 运行 `ruff check src tests` 和完整 `pytest -q`。
- [ ] 在固件目录执行 `idf.py build` 增量构建，不运行 `fullclean`。
- [ ] 检查两仓库 diff，确认未修改 HTTP 503/504 错误码和无关助教文件。
